<% if @project.save %>
  $('#usersTable').append("<%= j render('user_row', project: @project) %>");
  $('.close-modal').click();
<% else %>
  $('#errorArea').html("<%= j render('shared/errors', object: @project)%>")
<% end %>
